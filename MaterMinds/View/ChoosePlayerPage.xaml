<Page x:Class="MaterMinds.ChoosePlayerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MaterMinds"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="ChoosePlayerPage">
    <Border Background="White">
        <Grid>
            <TextBox HorizontalAlignment="Left" Margin="237,333,0,0" Text="{Binding Nickname}" TextWrapping="Wrap" VerticalAlignment="Top" Height="20" Width="120"/>
            <Button Content="Create Player" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="120" Height="40" Command="{Binding NewPlayer}" Margin="237,368,0,0"/>
            <Button Content="Select Player" HorizontalAlignment="Left" Margin="395,368,0,0" VerticalAlignment="Top" Height="40" Width="120" Command="{Binding ChoosePlayer}"/>
            <ListBox Margin="380,25,273,250" ItemsSource="{Binding PlayerList}" DisplayMemberPath="Nickname"/>
        </Grid>
    </Border>

</Page>
