<UserControl x:Class="MaterMinds.View.ChoosePlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaterMinds.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource BackgroundIMG}"
             >
    <Grid>
        <Grid>


            <Button Content="Main Menu" Margin="0,300,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" Command="{Binding Back}" Style="{StaticResource RoundButtonTemplate}" FontWeight="Bold" FontSize="25" Height="45" Width="319"  >
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="DodgerBlue"/>
                        <GradientStop Color="Black" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>

            <StackPanel Margin="350,0,0,50" VerticalAlignment="Center"  Width="300">
                <ListBox Margin="0,0,0,10" FontSize="25" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="Transparent" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Foreground="#b2cce5" SelectedItem="{Binding selectedPlayer}" ItemsSource="{Binding PlayerList}" DisplayMemberPath="Nickname" Height="150" />
                <Button Content="Select Player"  Command="{Binding ChoosePlayer}" VerticalAlignment="Top" Style="{StaticResource RoundButtonTemplate}" 
                            FontWeight="Bold" FontSize="20" Height="44">
                    <Button.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="DodgerBlue"/>
                            <GradientStop Color="Black" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>
            </StackPanel>
            <StackPanel Margin="0,0,350,50" VerticalAlignment="Center" Width="300">
                <Label x:Name="label" Content="Enter your nickname" FontWeight="Bold" Margin="0,0,0,40" Foreground="White" FontSize="25" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox Text="{Binding Nickname}" Margin="0,0,0,40" Background="Black" Foreground="#b2cce5" VerticalContentAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" BorderThickness="1" BorderBrush="White" FontSize="20" Height="41"/>
                <Button Content="Create Player" VerticalAlignment="Center" Command="{Binding NewPlayer}" Style="{StaticResource RoundButtonTemplate}" 
                    FontWeight="Bold" FontSize="20" Height="44">
                    <Button.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="DodgerBlue"/>
                            <GradientStop Color="Black" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>


                